# 长期投资基金日报设计

## 目标

本自动化用于在每个工作日收集半导体与黄金相关信息，并生成适合长期投资者每天阅读的执行型日报。

当前数据模型为 `long_term_review_v6`，不兼容旧逻辑。

报告每天只回答六个问题：

- 长期配置逻辑是否改变。
- 今天每只行动卡基金到底买入、卖出、持有、观望，还是今日不操作。
- 如果买入或卖出，本次建议金额是多少。
- 支撑这次动作的真实证据是什么。
- 最近 7 日判断有哪些有效、无效，以及今天要如何修正。
- 长期记忆里沉淀的判断是否还有效。

报告不是新闻列表，也不是盘中喊单。

固定行动卡基金：

- `012552 天弘芯片产业ETF联接A`：半导体核心配置。
- `021532 天弘半导体设备指数A`：半导体设备弹性仓。
- `000218 国泰黄金ETF联接A`：黄金防守仓，后续黄金新增只加到这只基金。

组合存量但不输出行动卡：

- `002611 博时黄金ETF联接C`：存量黄金仓，计入黄金总防守仓，不再建议加仓。

不再固定覆盖：

- `006533`

凡是报告中提到基金代码，必须同时带基金名称。

## 用户组合上下文

报告必须结合当前组合，不允许只看单只基金涨跌。

当前组合输入：

- 总资产约 `160000` 元。
- `012552 天弘芯片产业ETF联接A` 当前约 `11500` 元。
- `021532 天弘半导体设备指数A` 当前约 `4400` 元。
- `000218 国泰黄金ETF联接A` 当前约 `11100` 元。
- `002611 博时黄金ETF联接C` 当前约 `9600` 元，只保留，不加仓。
- 剩余约 `123400` 元在债基和货币基金。

组合计算要求：

- 半导体合计 = `012552 天弘芯片产业ETF联接A + 021532 天弘半导体设备指数A`。
- 黄金合计 = `000218 国泰黄金ETF联接A + 002611 博时黄金ETF联接C`。
- 债基/货币基金 = 总资产 - 半导体合计 - 黄金合计。
- `002611 博时黄金ETF联接C` 不进入实时行动链路，不输出行动卡，但必须影响黄金是否需要补仓的判断。
- 如果黄金总防守仓比例已经不低，`000218 国泰黄金ETF联接A` 不应因为自身单只金额偏低而自动补仓。

## 核心原则

### 用户动作优先

行动卡只允许展示用户可直接理解的动作：

- `买入`
- `卖出`
- `持有`
- `观望`
- `今日不操作`

旧行动词不再展示，也不再作为 `data.json` 主字段输出：

- `按原计划执行`
- `半额执行`
- `小额靠近`
- `暂停新增`
- `仅观察`
- `今日不输出执行建议`

如果内部实现仍需要节奏判断，只能作为推导过程使用，不能出现在报告主文案中。

### 金额必须来自当次信息

报告必须给出实际金额建议，不能只写“可买”“可卖”。

不得设置固定默认买入规则，例如“总资产 0.5%”。

金额建议必须由当次报告信息动态推导：

- 当前基金金额和占总资产比例。
- 所属资产组仓位，例如半导体合计和黄金合计。
- 盘中估算和底层代理是否显示追高、回落或失真。
- 正式净值的近 1 日、近 1 周、近 1 月表现。
- 最近 `1-2` 个月原始信息是否支持长期逻辑。
- 最近 7 日判断复盘是否提示需要收紧或修正。
- 长期记忆中已经验证或被证伪的判断。
- 该基金角色：核心仓、弹性仓、防守仓、存量仓。
- 交易确认时间差和未确认买入状态，但它们只作为生成上下文，不作为前台提醒。

行动卡可以给三个金额档位：

- `谨慎档`：适合不想承受更多波动的人。
- `均衡档`：本次报告认为更匹配当前证据和仓位的金额。
- `积极档`：只有长期逻辑强、仓位仍有空间、当日不追高时才给出。

必须同时给出 `recommended_amount`，即本次更建议执行的具体金额。

若动作是 `持有`、`观望` 或 `今日不操作`，金额必须为 `0`。

若动作是 `卖出`，金额用负数记录，例如 `-2000`，并说明卖出理由。卖出只在长期逻辑明显转弱、仓位显著超出约束或风险证据明确时出现。

### 证据支撑必须贴近动作

每张行动卡必须有 `evidence_support`，用于解释为什么是这个动作和金额。

证据可以来自：

- 原始长期信息，例如 WSTS、SIA、SEMI、World Gold Council、SAFE、U.S. Treasury、LBMA 等。
- 仓位证据，例如半导体合计占比、黄金合计占比。
- 行情证据，例如盘中估算、底层代理、近 1 日、近 1 周、近 1 月表现。
- 历史复盘证据，例如最近判断有效或失效的地方。
- 长期记忆证据，例如长期逻辑曾经被反复验证或需要降权的噪音。
- 判断逻辑，例如设备仓不能快速接近核心仓、黄金不是进攻仓。

证据必须服务于本次动作，不能堆砌无关数据。

### 长期逻辑优先

长期逻辑复核必须回答：

- 最近 `1-2` 个月有什么关键原始信息。
- 这些信息是否支持、削弱或改变 `6-24` 个月配置逻辑。
- 实际基金表现是否印证或提醒风险。
- 当前组合仓位是否允许继续执行。
- 最近 7 日判断复盘和长期记忆是否要求调整节奏。
- 因此今天动作和金额如何调整。

实时数据只做执行过滤器，不能单独改变长期逻辑。

### 交易确认时间差只是生成上下文

报告生成器必须知道基金买入不是实时按看到的价格成交，但这个信息不能作为页面提醒模块展示，也不能写成固定规则。

生成上下文必须包含：

- 报告生成时间。
- 通常确认截止时间。
- 基金买入通常按确认日净值成交，不是实时成交。
- 当前是否已知有未确认买入；如果未知，标记为 `unknown`。
- 给生成器的约束说明：生成动作和金额时要知道这些事实，但不要机械套用 `15:00` 前后规则。

这些上下文只在它们真实影响本次建议时，才可以自然进入“金额怎么来的”或“原因”里。若不影响建议，报告正文不展示。

### 正式净值作为表现数据

正式净值用于提供近 1 日、近 1 周、近 1 月表现，进入行动证据、最近 7 日复盘和组合仓位概览。

不得单独渲染“正式净值复核”页面层。
正式净值不得直接替代当天执行过滤器。

## 信息源策略

信息源不再固定只从少数几个来源抓取，也不允许让模型无限制网页搜索。

仓库根目录必须有 `sources-registry.json`，作为可用信息源注册表。报告生成时必须读取该文件，并根据当次组合、近期复盘、长期记忆和数据可用性选择本轮来源。

信息源分层：

- 固定行情和基金数据：天天基金估值接口、东方财富历史净值接口、腾讯行情接口。
- 半导体核心来源：WSTS、SIA、SEMI、国内政策和监管来源。
- 半导体设备来源：SEMI、设备资本开支、国产替代政策和监管来源。
- 黄金核心来源：World Gold Council、SAFE、LBMA、U.S. Treasury 实际利率、美元指数或美元流动性来源、黄金 ETF 持仓流向。

每次报告必须输出 `source_selection`：

- 本轮选中了哪些来源。
- 选中原因。
- 是否实际抓取成功。
- 证据质量。
- 没有选中或抓取失败的来源为什么不进入结论。
- 需要新增但还未接入的候选来源。

AI 可以提出 `proposed_sources`，但只能作为候选，不能直接混入当次证据。候选来源必须有用途、可靠性、接入方式和为什么比现有来源更有价值。

## 历史报告与长期记忆

输出目录结构：

```text
reports/
  YYYY-MM-DD/
    data.json
    report.html
    sources.md
  long-term-memory.md
```

每次运行必须：

- 生成或覆盖 `reports/YYYY-MM-DD/`。
- 日期目录直接放在 `reports/` 下，按日期排序即可。
- 最多保留最近 7 个日期目录。
- 超过 7 日的日期目录必须先压缩进 `reports/long-term-memory.md`，再删除。

生成今日报告前，必须读取：

- `design.md`
- `sources-registry.json`
- `reports/long-term-memory.md`
- 最近 7 个日期目录下的 `data.json`
- 最近 7 个日期目录下的 `report.html`
- 最近 7 个日期目录下的 `sources.md`

最近 7 日判断复盘必须在生成今日报告前完成，并写入 `data.json.history_review`，再渲染进今日 `report.html`。

复盘不叫“预测准确率”，统一叫“判断有效率”。

`history_review` 必须包含：

- `effectiveness_pct`：判断有效率，样本不足时为 `null`。
- `sample_size`：纳入复盘的行动判断数量。
- `basis`：复盘口径。
- `accurate_points`：判断有效的地方。
- `inaccurate_points`：判断失效或证据不足的地方。
- `follow_up_adjustments`：今天要如何修正动作、金额或信息源。
- `review_items`：逐条复盘明细。

`reports/long-term-memory.md` 必须持续维护：

- 当前长期结论。
- 已验证有效的判断。
- 历史判断偏差。
- 资金与仓位经验。
- 来源可靠性。
- 已删除或降权的噪音。
- 最近压缩更新记录。

长期记忆不是简单追加日志。每次生成报告后都要压缩、去噪和重写该文件，保留有价值的长期信息。

## 运行时间

- 频率：每个工作日。
- 建议时间：上午 `10:15-10:30`，默认时区 `Asia/Shanghai`。
- 也允许晚上手动拉取报告，用来结合当日最新数据决定是否行动。
- 若遇中国内地基金市场休市日，仍可生成报告，但必须标注休市，并且不输出当天执行建议。

## 数据结构

`data.json` 必须先生成，`report.html` 和 `sources.md` 只能从 `data.json` 渲染。

当前数据模型：

```json
{
  "model_version": "long_term_review_v6",
  "report_date": "2026-06-12",
  "generated_at": "2026-06-12T02:30:00.000Z",
  "automation_context": {},
  "data_quality": {},
  "portfolio_context": {},
  "execution_context": {},
  "history_context": {},
  "history_review": {},
  "long_term_memory": {},
  "top_conclusion": {},
  "thesis_reviews": {},
  "funds": [],
  "source_registry": {},
  "source_selection": {},
  "source_pool": {},
  "info_source_errors": []
}
```

旧字段 `portfolio_thesis`、`decision_status`、`decision_feed` 不再使用。

旧行动字段 `execution_plan`、`trigger_check` 不再作为主输出使用。

### `top_conclusion`

```json
{
  "status": "green | yellow | red",
  "status_label": "可执行 | 收紧 | 暂停",
  "headline": "今日建议：买入半导体，不新增黄金",
  "action_summary": "012552 天弘芯片产业ETF联接A 买入 800 元；021532 天弘半导体设备指数A 买入 400 元；000218 国泰黄金ETF联接A 持有 0 元",
  "total_buy_amount": 1200,
  "total_sell_amount": 0,
  "focus": ["..."],
  "data_status": "实时链路完整，可信度 100%"
}
```

顶部结论不得重复展示组合仓位摘要。组合仓位放到页面最后。

### `action_plan`

每只行动卡基金必须包含：

```json
{
  "action": "buy | sell | hold | watch | no_action",
  "action_label": "买入 | 卖出 | 持有 | 观望 | 今日不操作",
  "amount_tiers": [
    { "label": "谨慎档", "amount": 500, "description": "..." },
    { "label": "均衡档", "amount": 800, "description": "..." },
    { "label": "积极档", "amount": 1200, "description": "..." }
  ],
  "recommended_amount": 800,
  "recommended_label": "本次更建议 800 元",
  "amount_rationale": "仓位仍有空间，盘中位置可接受，所以采用均衡档。",
  "reason": "...",
  "evidence_support": []
}
```

行动卡不得包含“什么情况改主意”或“停止条件”。
行动卡不得包含提醒式交易文案。
若交易确认时间差确实改变本次金额，只允许自然写进 `amount_rationale`。

### `execution_context`

```json
{
  "generated_at_local": "2026-06-12 18:00:00",
  "order_cutoff_time": "15:00",
  "is_after_cutoff": true,
  "trade_confirmation_fact": "基金买入通常按确认日净值成交，不是实时成交。",
  "pending_order_status": "unknown",
  "model_instruction": "生成动作和金额时必须知道净值确认时间差、未确认买入缺口和下一交易日风险，但不要把这些事实作为固定规则或提醒文案输出。"
}
```

`execution_context` 是给生成逻辑看的上下文，不是 HTML 主报告模块。

### `portfolio_context`

```json
{
  "total_assets": 160000,
  "as_of": "user_supplied_static",
  "positions": [],
  "groups": [],
  "cash_like": {},
  "notes": []
}
```

每个 `positions` 条目必须包含：

- 基金代码和基金名称。
- 当前金额和占总资产比例。
- 今日估算涨跌。
- 近 1 日、近 1 周、近 1 月表现。
- 当前状态说明。

## 新鲜度规则

每条实时或准实时数据都必须记录：

- `data_time`
- `fetch_time`
- `freshness_minutes`
- `usable_for_today_execution`
- `source_name`
- `source_url`

默认门槛：

- 基金盘中估算：`10` 个有效交易分钟内。
- 底层实时代理：`5` 个有效交易分钟内。
- 黄金 ETF 或黄金现货代理：`5` 个有效交易分钟内。
- 正式净值不限新鲜度，但只能用于复核。

## 基金规则

### `012552 天弘芯片产业ETF联接A`

定位：半导体核心配置。

- 长期逻辑为 `green` 且实时链路完整时，可以输出 `买入`。
- 当前半导体总仓位约 `9.9%`，仍属于中等偏低配置，核心仓可作为半导体主要新增入口。
- 盘中明显上涨时，金额档位要收紧。
- 盘中小幅回落且长期逻辑未变时，可以提高均衡档，但不能写成重仓信号。
- 长期逻辑为 `yellow` 时，动作应偏 `观望` 或低金额 `买入`。
- 长期逻辑为 `red` 时，动作应为 `今日不操作` 或 `卖出`，取决于风险证据。

### `021532 天弘半导体设备指数A`

定位：半导体设备弹性仓。

- 执行约束必须比 `012552 天弘芯片产业ETF联接A` 更严格。
- 当前设备仓约 `2.8%`，可以保留弹性，但不应快速放大到接近核心仓。
- 设备链上涨明显时，优先降低金额或观望。
- 设备链回落时也只能给小额，因为波动和回撤都更大。
- 只有设备国产化、行业景气或政策逻辑继续增强时，才允许输出小额 `买入`。

### `000218 国泰黄金ETF联接A`

定位：黄金防守仓。

- 黄金不是进攻仓，不因单日下跌自动补仓。
- `000218 国泰黄金ETF联接A` 当前约 `6.9%`，但黄金合计含 `002611 博时黄金ETF联接C` 后约 `12.9%`。
- 后续黄金新增只加到 `000218 国泰黄金ETF联接A`。
- 若黄金总防守仓比例不低，动作应偏 `持有` 或 `观望`，金额为 `0`。
- 只有黄金总防守仓比例不足，且实时数据没有显示极端失真时，才考虑小额 `买入`。
- 若美元、美债收益率或实际利率信号明显压制黄金，动作偏 `观望` 或 `今日不操作`。

## HTML 输出结构

### 第一层：顶部结论

必须包含：

- 抓取时间。
- 数据质量状态和可信度百分比。
- 今日结论。
- 建议买入总金额和卖出总金额。
- 每只行动卡基金的动作和金额。

不得包含组合仓位摘要。
不得包含提醒式交易规则说明。

### 第二层：今日操作建议

必须展示三张行动卡：

- `012552 天弘芯片产业ETF联接A`
- `021532 天弘半导体设备指数A`
- `000218 国泰黄金ETF联接A`

每张卡必须包含：

- 动作。
- 谨慎档、均衡档、积极档。
- 本次更建议金额。
- 金额怎么来的。
- 原因。
- 证据支撑。

不得包含“停止条件”或“什么情况改主意”。
不得包含提醒式交易规则说明。

### 第三层：近 7 日判断复盘

必须展示：

- 判断有效率。
- 样本数量。
- 复盘口径。
- 判断有效的地方。
- 判断不准确或证据不足的地方。
- 今天要如何修正。

样本不足时要明确写“样本不足”，不能伪造百分比。

### 第四层：长期逻辑复核

默认展示：

- 结论。
- 是否改变配置逻辑。
- 当前仓位含义。
- 对今天动作和金额的影响。
- 后续观察项。

默认折叠：

- 最近 `1-2` 个月发生了什么。
- 原始信息证据。
- 结合基金表现怎么看。

### 第五层：来源与不确定性

必须说明：

- 本轮来源选择依据。
- 实时数据来源。
- 基金表现数据来源。
- 长期复核证据来源。
- 生成上下文摘要，说明交易确认时间差已作为背景输入。
- 最近 7 日历史报告和长期记忆是否可用。
- 数据缺口。
- 今日金额建议的主要不确定性。

### 第六层：组合仓位概览

放在最后，作为背景材料。

必须展示：

- 总资产估算。
- 半导体合计金额和占比。
- 黄金合计金额和占比，并标注 `002611 博时黄金ETF联接C` 只保留不加仓。
- 债基/货币基金金额和占比。
- 每只组合基金的当前金额、占比、今日估算、近 1 日、近 1 周、近 1 月、当前状态。

## `sources.md`

必须记录：

- 本轮来源选择。
- 来源注册表版本。
- 实时执行过滤器来源。
- 基金表现数据来源。
- 长期逻辑复核证据来源。
- 最近 7 日历史报告来源。
- 长期记忆来源。
- 组合仓位来源。
- 生成上下文来源。
- 金额建议说明。
- 数据时间、抓取时间、新鲜度、是否可用于今日执行。
- 可信度备注。

## 写作要求

- 中文撰写。
- 面向非专业投资者，尽量白话。
- 先给结论，再给依据。
- 每段尽量不超过 `120` 个中文字。
- 区分事实、推断和不确定性。
- 不写盘中喊单。
- 不把数据新鲜度写成投资理由。
- 不把长期配置建议和当天动作混为一谈。
- 不把 `000218 国泰黄金ETF联接A` 单只占比和黄金总仓位混淆。
- 不把交易确认时间差写成前台提醒；只在它影响本次金额时自然解释。

## 自动化执行步骤

每次运行必须：

1. 读取本设计文档。
2. 读取 `sources-registry.json`。
3. 读取 `reports/long-term-memory.md`，若不存在则创建初始文件。
4. 读取最近 7 个日期目录下的 `data.json`、`report.html`、`sources.md`。
5. 抓取实时执行过滤器数据、正式净值、原始信息证据。
6. 计算新鲜度并生成 `data.json`。
7. 生成 `portfolio_context` 和 `execution_context`。
8. 生成 `history_review`，并在今日结论和金额中使用复盘结果。
9. 生成 `thesis_reviews`，把原始信息、基金表现、组合仓位、最近复盘和长期记忆合并成长期逻辑复核。
10. 生成每只基金的 `action_plan`，包含动作、金额档位、本次建议金额、金额原因和证据支撑。
11. 生成不重复仓位摘要的 `top_conclusion`。
12. 渲染 `report.html`。
13. 渲染 `sources.md`。
14. 将三份文件同步写入 `reports/YYYY-MM-DD/`。
15. 压缩并重写 `reports/long-term-memory.md`。
16. 保留最近 7 个日期目录，删除更早目录。
17. 自检三份文件是否一致。

## 自检清单

- 是否读取 `design.md`、`sources-registry.json`、长期记忆和最近 7 日日期目录。
- 是否生成 `model_version: long_term_review_v6`。
- 是否生成或覆盖 `reports/YYYY-MM-DD/` 三份文件。
- 是否保留不超过 7 个日期目录。
- 是否生成并更新 `reports/long-term-memory.md`。
- 是否生成 `history_review`，且 HTML 第三层展示近 7 日判断复盘。
- 是否只输出 `012552 天弘芯片产业ETF联接A`、`021532 天弘半导体设备指数A`、`000218 国泰黄金ETF联接A` 三张行动卡。
- 是否不再依赖 `portfolio_thesis`、`decision_status`、`decision_feed`。
- 是否不再展示旧行动词。
- 是否每张行动卡都有动作、金额档位、本次建议金额、金额原因、原因和证据支撑。
- 是否不再展示“停止条件”或“什么情况改主意”。
- 是否不再展示提醒式交易规则说明。
- 是否生成 `execution_context`，且不把它作为 HTML 主模块渲染。
- 是否把行动卡放在第二层。
- 是否把近 7 日判断复盘放在第三层。
- 是否把组合仓位概览放在最后。
- 是否所有基金代码都带基金名称。
- 是否把 `002611 博时黄金ETF联接C` 计入黄金总仓位但不输出行动卡。
- 长期逻辑复核是否结论先行、证据折叠。
- 是否不再渲染“正式净值复核”独立页面层。
- 来源与不确定性是否披露来源选择、组合仓位来源、金额建议说明、历史报告和长期记忆状态。
